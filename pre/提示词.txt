第一张图：
一张横版古风科技感数据处理架构图。宣纸纹理的深红褐色背景。

画面中下部从左到右排列四个阶段区域，构成一条横向数据流水线。每个阶段是一个金色细线边框的圆角矩形卡片，阶段之间由金色粗箭头水平串联。每个卡片内以层级文字呈现。

——

阶段一（最左）：
标题（象牙白，稍大）：① 原始数据采集
下方细项（象牙白，稍小）：
· 76集 MP3 音频
· 44.1kHz Stereo 128kbps
· 已去片头片尾
· 原始总时长约 80 小时
底部标注（金色小字）：数据源：《甄嬛传》全集原声

阶段二：
标题（象牙白，稍大）：② UVR5 人声分离
下方细项：
· UVR5 demucs（mdx_extra_q）
· → 纯净人声 44.1kHz Stereo
底部标注（金色小字）：去除背景音乐与环境噪声

阶段三：
标题（象牙白，稍大）：③ VAD 切分 + ASR 转写
下方细项：
· FunASR SeacoParaformer（VAD+ASR 二合一）
· 时间戳合并（间隔 >800ms 切分）
· → 15,685 段 WAV（16kHz Mono / 2~15s）

阶段四（最右）：
标题（象牙白，稍大）：④ 声纹提取 + 说话人聚类
下方细项：
· eres2netv2 → Speaker Embedding（512维 × 15,685 段）
· KMeans 聚类（n=30）
· → 30 个匿名说话人簇（spk_00 ~ spk_29）

——

四个阶段均匀分布在画面中部偏下区域，上方留白供 PPT 标题和说明文字叠加。

整体极简克制，大面积留白，无人物、无场景装饰、无多余图案。配色严格限定：底色深红褐（#8B4513），所有边框、箭头连线、分隔线为宫廷金（#D4AF37），正文内容为象牙白（#FFFFF0）。金色箭头粗而清晰，箭头方向统一向右。每个阶段卡片背景为半透明深色，与底色形成微妙层次。16:9 横版构图。

第二张图：
一张正方形古风科技感数据清洗流程图。宣纸纹理的深红褐色背景。

画面中央从上到下排列 4 行内容，每行为一个清洗步骤。整体呈三列布局：左列宽（约占画面 60%），中列窄（约占 20%），右列窄（约占 20%）。三列之间无框线分隔，仅靠空间留白和自然对齐区分。

左列：每行步骤名使用宫廷金（#D4AF37），描述文字使用象牙白（#FFFFF0）——
第 1 行：角色过滤 —— 剔除不属于 14 个目标角色的非目标人物片段
第 2 行：文本过滤 —— 剔除 ASR 转写为空、过短（<2 字）或过长（>80 字）的低质量片段
第 3 行：文本去重 —— 相同台词仅保留一条，去除剧中重复台词造成的冗余
第 4 行：音频质检 —— 剔除静音片段（RMS < 0.003）与削波严重片段（削波率 > 10%）

中列（数据递减链，全部使用宫廷金 #D4AF37，与左列逐行对齐）：
15,685
  ▼
14,003
  ▼
13,644
  ▼
13,613
  ▼
13,605

上下两个数字之间仅靠金色向下箭头连接串联，无框线。最底部 13,605 字号稍大并加金色光晕高亮。

右列（丢弃数目，象牙白 #FFFFF0，与左列逐行对齐）：
第 1 行：-1,682
第 2 行：-359
第 3 行：-31
第 4 行：-8
底部（宫廷金）：合计 -2,080

——

画面仅保留一条金色细线圆角矩形外框作为整体收边，内部无任何网格线、无竖线分隔、无横线分割。各行之间依靠均匀的行间距自然呼吸。

左上角一枚金色小标签，象牙白文字"数据清洗 Pipeline"。右下角一行金色高亮标注"最终：14 角色 · 13,605 段 · 16.7h"。

整体极简克制，通透疏朗，无人物、无场景装饰、无多余图案。配色严格限定：底色深红褐（#8B4513），外框、箭头和关键标注为宫廷金（#D4AF37），步骤名统一为宫廷金，描述文字和丢弃数目为象牙白（#FFFFF0），中列数字递减链为宫廷金。正方形 1:1 构图。
